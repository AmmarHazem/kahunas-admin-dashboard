import { FC } from "react";

const Loading: FC = () => {
  return (
    <div className="bg-blue-500 px-8 py-3 text-white fixed top-0 left-0 w-full h-[10px] text-sm flex items-center justify-center z-20">
      Loading
    </div>
  );
};

export default Loading;
